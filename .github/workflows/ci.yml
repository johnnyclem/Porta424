name: CI

on:
  pull_request:
  push:
    branches: [ "main" ]

jobs:
  linux:
    name: Linux (ubuntu-22.04)
    # Pinned to 22.04: swift-actions/setup-swift@v1 does not support the
    # Ubuntu 24.04 image that `ubuntu-latest` now resolves to.
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Set up Swift
        uses: swift-actions/setup-swift@v1
        with:
          swift-version: "5.9"
      - name: Build
        run: swift build --build-tests
      - name: Test
        run: swift test

  macos:
    name: macOS (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build --build-tests
      - name: Test
        run: swift test
